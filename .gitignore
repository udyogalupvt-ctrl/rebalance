# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment — never commit. Copy .env.example and fill in locally.
.env
.env.*
!.env.example

# Firebase Admin SDK service account keys.
# These grant FULL admin access to the project and bypass every security
# rule. They are server-side only and must never be committed or bundled.
*-firebase-adminsdk-*.json
serviceAccount*.json
